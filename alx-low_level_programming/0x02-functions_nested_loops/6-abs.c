#include <stdlib.h>

/**
 * _abs - finds absolute value of a number
 * Description: function finds and prints the absolute value of a number
 * @n: number argument to find
 * Return: returns the absolute value of a number
 */
int _abs(int n)
{
return (abs(n));
}

