#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string follows by a new line
 * Description: function prints a string to stdout, followed by a new line
 * @str: string to print
 */

void _puts(char *str)
{
puts(str);
}
