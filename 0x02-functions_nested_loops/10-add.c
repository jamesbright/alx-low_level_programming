#include <stdlib>

/**
 * add - adds two numbers
 * Description: function adds two numbers and returns the result
 * @a: first value
 * @b: second value
 * Return: return the result of the two numbers added
 */
 
 int add(int a, int b)
 {
int sum = a + b;
return (sum);
 }
