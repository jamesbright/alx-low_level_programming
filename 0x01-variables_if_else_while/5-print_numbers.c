#include <stdlib.h>
#include <stdio.h>
#include <string.h>
/**
 * main - program prints numbers and returns 0
(* a blank line
 * Descriptrints numbers 0-9)?
 * a text is printed after the code is executed
 * Return: returns 0 at end of execution
 */
int main(void)
{
unsigned int i;
for (i = 0; i <= 9; ++i)
{
putchar(i);
}
putchar('\n');
return (0);
}
