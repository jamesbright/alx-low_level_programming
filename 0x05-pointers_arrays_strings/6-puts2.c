#include "main.h"

/**
 * puts - function to print character
 * @str: variable
 * Description: function to print all characters in a
 */

void puts(char *str)
{
int i = 0;
while (*(s + i))
{

if (i == 0)
_putchar(*(s + i));

_putchar(*(s + i + 1));

}
}
