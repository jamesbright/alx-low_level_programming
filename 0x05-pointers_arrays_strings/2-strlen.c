#include "main.h"
#include <string.h>
/**
 * _strlen - returns length of a string
 * Description: counts and returns the length of a string
 * @s: string to be returned
 * Return: length of s
 */

int _strlen(char *s)
{
return (strlen(s));
}
